<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ICT PDSI Utility v1.0 - Windows Technician Suite" Height="720" Width="1100"
        Background="#F3F4F6" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>
        
        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="ICT PDSI UTILITY v1.0" Foreground="#38BDF8" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="  |  Workstation Technician Tool" Foreground="#94A3B8" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="TxtHostInfo" Text="Host: - | Domain: -" Foreground="#CBD5E1" FontSize="12" FontFamily="Consolas" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
        
        <!-- Main Tab Control -->
        <TabControl Grid.Row="1" Background="White" BorderBrush="#E2E8F0">
            <!-- TAB 1: Software Deployment -->
            <TabItem Header="Software Deployment">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Action Bar -->
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <TextBlock Text="Software Batch Installer Queue" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <Button x:Name="BtnSelectAll" Content="Select All" Width="90" Height="28" Margin="0,0,5,0" Background="#E2E8F0"/>
                            <Button x:Name="BtnDeselectAll" Content="Deselect All" Width="90" Height="28" Margin="0,0,5,0" Background="#E2E8F0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                            <Button x:Name="BtnInstallSelected" Content="⚡ Install Selected" Width="140" Height="30" Background="#0284C7" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </DockPanel>
                    
                    <!-- App List DataGrid -->
                    <DataGrid x:Name="GridApps" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" 
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9"
                              Background="White" RowHeaderWidth="0" FontSize="12">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, Mode=TwoWay}" Width="60"/>
                            <DataGridTextColumn Header="Software Name" Binding="{Binding Name}" Width="2*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Est. Size" Binding="{Binding EstimatedMB}" Width="80"/>
                            <DataGridTextColumn Header="Installer File" Binding="{Binding InstallerFileName}" Width="1.5*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- TAB 2: Admin Tools -->
            <TabItem Header="Admin Tools">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Windows Administrative Commands" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        
                        <!-- Hostname & Domain Section -->
                        <GroupBox Header="Computer Identification &amp; Domain" Padding="10" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="New Computer Name (Hostname):" Margin="0,0,0,5"/>
                                    <TextBox x:Name="TxtNewHostname" Height="28" Padding="4" Margin="0,0,0,8"/>
                                    <Button x:Name="BtnRenameHost" Content="Rename Hostname" Height="30" Background="#0EA5E9" Foreground="White"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Target Domain Name:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="TxtTargetDomain" Text="PDSI.CORP.LOCAL" Height="28" Padding="4" Margin="0,0,0,8"/>
                                    <Button x:Name="BtnJoinDomain" Content="Join Domain" Height="30" Background="#0284C7" Foreground="White"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                        
                        <!-- Network Utilities -->
                        <GroupBox Header="Network &amp; Stack Reset" Padding="10">
                            <WrapPanel>
                                <Button x:Name="BtnFlushDns" Content="Clear DNS Cache" Width="150" Height="34" Margin="5" Background="#F1F5F9"/>
                                <Button x:Name="BtnResetWinsock" Content="Reset Winsock &amp; IP" Width="150" Height="34" Margin="5" Background="#F1F5F9"/>
                                <Button x:Name="BtnPingDomain" Content="Test Ping Domain" Width="150" Height="34" Margin="5" Background="#F1F5F9"/>
                                <Button x:Name="BtnGpUpdate" Content="Run Group Policy Update" Width="170" Height="34" Margin="5" Background="#F1F5F9"/>
                            </WrapPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- TAB 3: Tweaks & Fixes -->
            <TabItem Header="Tweaks &amp; Fixes">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Performance &amp; Maintenance Optimizations" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        <WrapPanel>
                            <Button x:Name="BtnCleanTemp" Content="🧹 Clean Temp Files" Width="200" Height="40" Margin="6" Background="#F1F5F9" HorizontalContentAlignment="Left" Padding="10,0,0,0"/>
                            <Button x:Name="BtnSfcScan" Content="🛡️ System File Check (SFC)" Width="200" Height="40" Margin="6" Background="#F1F5F9" HorizontalContentAlignment="Left" Padding="10,0,0,0"/>
                            <Button x:Name="BtnPowerPlan" Content="⚡ High Performance Power" Width="200" Height="40" Margin="6" Background="#F1F5F9" HorizontalContentAlignment="Left" Padding="10,0,0,0"/>
                            <Button x:Name="BtnDisableTelemetry" Content="🔒 Disable Telemetry" Width="200" Height="40" Margin="6" Background="#F1F5F9" HorizontalContentAlignment="Left" Padding="10,0,0,0"/>
                            <Button x:Name="BtnRestartExplorer" Content="🔄 Restart Windows Explorer" Width="200" Height="40" Margin="6" Background="#F1F5F9" HorizontalContentAlignment="Left" Padding="10,0,0,0"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- TAB 4: System Information -->
            <TabItem Header="System Information">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Hardware &amp; Workstation Diagnostics" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Button x:Name="BtnRefreshSysInfo" Content="🔄 Refresh Diagnostics" Width="150" Height="28" DockPanel.Dock="Right" HorizontalAlignment="Right" Background="#F1F5F9"/>
                    </DockPanel>
                    <TextBox x:Name="TxtSysDetails" Grid.Row="1" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                             FontFamily="Consolas" FontSize="12" Background="#F8FAFC" Padding="10" BorderBrush="#E2E8F0"/>
                </Grid>
            </TabItem>
        </TabControl>
        
        <!-- Terminal / Log Output Footer -->
        <Border Grid.Row="2" Background="#0F172A" CornerRadius="6" Padding="10" Margin="0,12,0,0">
            <TextBox x:Name="TxtConsole" Background="Transparent" Foreground="#4ADE80" FontFamily="Consolas" FontSize="11"
                     IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                     Text="[PDSI System] Utility initialized. Ready for technician input." BorderThickness="0"/>
        </Border>
    </Grid>
</Window>
